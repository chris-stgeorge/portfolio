import { defineConfig } from 'astro/config';

// This is Astro's config file. You won't need to touch this for now.
// Later, if you deploy your site, you can set "site" to your real URL —
// it's used for things like sitemaps, and isn't required to get started.
export default defineConfig({});
